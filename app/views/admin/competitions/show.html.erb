<div class="container">
    <div class="row no1">
        <div class="col-xs-6">
            <table>
                <tr>
                    <th>大会名名</th>
                    <td><%= @competition.name %></td>
                </tr>
                <tr>
                    <th>内容</th>
                </tr>
                <tr>
                    <td><%= @competition.summary.html_safe %></td>
                <tr>
                <tr>
                    <th>ステータス</th>
                    <%if @competition.is_valid == true %>
                    <td> 公開 </td>
                    <% else %>
                    <td> 非公開 </td>
                    <% end %>
                </tr>
                        <tr>
                        <td><%= link_to "edit", edit_admin_competition_path(@competition) %></td>
                        </tr>
                        <tr>
                            <td> <%= link_to "Destroy", admin_competition_path(@competition.id), method: :delete,  "data-confirm" => "本当に削除しますか？"  ,class:"btn-sm btn-danger destroy_menu_10349"%></td>
                        </tr>
            </table>
        </div>
        <div class="col-xs-3">
        記録一覧

        
        </div>

    </div>
<div>
<div class="container">
    <div class="row no1">
        <div class="col-xs-6">
            <table>
                <tr>
                    <th>大会名名</th>
                    <td><%= @competition.name %></td>
                </tr>
                <tr>
                    <th>内容</th>
                </tr>
                <tr>
                    <td><%= @competition.summary.html_safe %></td>
                <tr>
                <tr>
                    <th>ステータス</th>
                    <%if @competition.is_valid == true %>
                    <td> 公開 </td>
                    <% else %>
                    <td> 非公開 </td>
                    <% end %>
                </tr>
                <tr>
                    <td><%= link_to "edit", edit_admin_competition_path(@competition) %></td>
                </tr>
                <tr>
                    <td> <%= link_to "Destroy", admin_competition_path(@competition.id), method: :delete,  "data-confirm" => "本当に削除しますか？"  ,class:"btn-sm btn-danger destroy_menu_10349"%></td>
                </tr>
              
            </table>
        </div>
        <div class="col-xs-3">


        記録一覧
        <%= form_for @record, url: records_path, method: :post, class: "form-control" do |f| %>
                
            <%= f.label :大会名 %>
                <%= f.collection_select :competition_id, @user.competitions.all, :id, :name %><br>
            
                <%= f.label :種目 %>
                <%= f.collection_select :athletic_event_id, AthleticEvent.all, :id, :name %><br>

                <%= f.label :選手名 %>
                <%= f.text_area :name %><br>
    
                <%= f.label :記録 %>
                <%= f.text_area :time %>
   
                <%= f.submit "投稿", class:"btn btn-lg btn-block btn-base-1 mt-20" %>
        <% end %>

        <div class="container">
<div class="row no1">
    <div class="col-xs-3">
    </div>
    <div class="col-xs-3">
        <h2>記録一覧</h2>
        <div class="record-main">
            <table class="table table-striped">
             　<tr>
                <thead>
                    <th>大会一覧</th>
                </thead>
                </tr>
                <tr>
                    <% @records.each do |record| %>
                    <td>
                     <%= link_to record.name, record_path(record) %>
                    </td>
                    <% end %>
                </tr>
        </table>
        

    </div>
    </div>
</div>
</div>

        
        </div>

    </div>
<div>