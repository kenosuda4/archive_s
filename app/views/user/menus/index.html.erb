<div class="row">

        <div class="col-xs-3">
            
        </div>
        <div class="col-xs-9">
                <% @menus.each do |menu| %>
                        <%= link_to menu_path(menu), class: "btn-link" do %>
                                <%= menu.name %>
                        <% end %>
                        <%= menu.user.name %>
                <% end %>
        </div>
    

</div>